python 中一切皆是对象：数值，字符串，列表，元祖，集合，函数，类，模块;
运行虚拟环境：source venv/bin/activate;
退出虚拟环境：deactivate;
is比较的是内存地址，==比较的值相等；

酸洗和去除：Pickle模块接受任何Python对象并将其转换为字符串表示形式，并使用dump函数将其转储到文件中，此过程称为pickling.
          从存储的字符串中检索原始Python对象的过程称为unpickling

